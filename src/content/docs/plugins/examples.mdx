---
title: Plugin Examples
description: Ready-to-use and community plugin examples for semrel.
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components';

## Official plugins

Official plugins are published in the [SemRels](https://github.com/SemRels) organization.

<CardGrid>
  <Card title="github" icon="github">
    **ProviderPlugin** for GitHub. Creates releases, uploads assets, and fetches commit history via the GitHub REST API.
  </Card>
  <Card title="conventional-commits" icon="magnifier">
    **CommitAnalyzerPlugin** that follows the [Conventional Commits](https://www.conventionalcommits.org/) specification to determine the version bump.
  </Card>
  <Card title="markdown-changelog" icon="document">
    **ChangelogGeneratorPlugin** that renders release notes as a Markdown `CHANGELOG.md` section.
  </Card>
  <Card title="go-mod-updater" icon="pencil">
    **FilesUpdaterPlugin** that stamps the next version into Go source files via `ldflags`-compatible version variables.
  </Card>
  <Card title="packager-nfpm" icon="package">
    **PackagerPlugin** that builds Linux packages (`deb`, `rpm`, `apk`) through `nfpm`.
  </Card>
  <Card title="publisher-oci" icon="upload">
    **PublisherPlugin** that uploads artifacts to OCI registries with `oras push`.
  </Card>
  <Card title="publisher-generic-http" icon="upload">
    **PublisherPlugin** that uploads artifacts to custom HTTP endpoints.
  </Card>
</CardGrid>

<Aside type="note">
  All official plugins are open source and available in the [SemRels](https://github.com/SemRels) organization. Install any plugin with `semrel plugin install <name>`.
</Aside>

## Practical release-chain examples

### Go binaries + package artifacts + OCI publishing

```yaml
plugins:
  - uses: updater-go
    args:
      file: internal/version/version.go
      var_name: Version

  - uses: packager-nfpm
    args:
      config: packaging/nfpm.yaml
      target: dist/packages
      packagers: deb,rpm,apk

  - uses: publisher-oci
    args:
      ref: ghcr.io/semrels/myapp:{version}
      artifacts: dist/packages/myapp_amd64.deb,dist/packages/myapp_amd64.rpm
```

### Docker or Podman image workflow

```yaml
plugins:
  - uses: updater-docker
    args:
      file: Dockerfile
      arg_name: VERSION

  - uses: publisher-generic-http
    args:
      url: https://uploads.example.com/releases/{version}/{artifact}
      method: PUT
      token: ${UPLOAD_TOKEN}
      artifacts: dist/images/myapp-oci.tar
```

### Helm chart workflow

```yaml
plugins:
  - uses: updater-helm
    args:
      file: charts/myapp/Chart.yaml
      update_app_version: "true"

  - uses: publisher-oci
    args:
      ref: ghcr.io/semrels/charts/myapp:{version}
      artifacts: dist/charts/myapp-{version}.tgz
```

## Community plugins

Community-contributed plugins will be listed in the [semrel registry](https://registry.semrel.io/) once it launches. To have your plugin listed, open an issue or PR in the main repository.
