---
title: Plugin Migration Guide
description: Migration hub for moving from semantic-release, GoReleaser, and release-please to semrel.
---

{/* This code has been partially generated by an AI. */}

import { Aside, Steps, LinkButton } from '@astrojs/starlight/components';

Use this hub to select the migration path that matches your current release tool.

## Choose your source tool

| Current tool | Best migration page | Typical migration priority |
| --- | --- | --- |
| semantic-release | [From semantic-release to semrel](/plugins/migrations/semantic-release/) | analyzer + generators first, then publisher parity |
| GoReleaser | [From GoReleaser to semrel](/plugins/migrations/goreleaser/) | build matrix parity, then packaging/publishing split |
| release-please | [From release-please to semrel](/plugins/migrations/release-please/) | preserve PR visibility, then switch tag/release owner |

<LinkButton href="/plugins/migrations/semantic-release/" icon="book">semantic-release Migration</LinkButton>
<LinkButton href="/plugins/migrations/goreleaser/" variant="secondary" icon="book">GoReleaser Migration</LinkButton>
<LinkButton href="/plugins/migrations/release-please/" variant="secondary" icon="book">release-please Migration</LinkButton>

## Shared migration baseline

All migration paths converge to the same semrel model:

- Updaters prepare version metadata in source/config files
- Packagers build distributable artifacts
- Publishers upload artifacts to registries or HTTP endpoints

## Migration Strategy

<Steps>

1. Keep existing updater plugins in place.
2. Add a build stage in CI that produces real release artifacts (`dist/`, `charts/`, image tarballs).
3. Add packager/publisher plugins after build artifacts exist.
4. Run first in dry-run mode and compare outputs with your current release flow.
5. Enable full release and monitor the first 2-3 releases.

</Steps>

<Aside type="tip">
  Start by migrating one artifact type first (for example Linux packages), then add OCI or HTTP publishing in a second step.
</Aside>

## Validation Checklist

- `semrel --dry-run release` prints expected plugin order and artifact paths.
- Build artifacts exist before publisher plugins run.
- Registry/endpoint credentials are provided via environment variables.
- Release notes and changelog still contain expected content.
- Rollback path exists (keep previous release workflow for one cycle).

## Related docs

<LinkButton href="/plugins/overview/" icon="list-format">Plugin Overview</LinkButton>
<LinkButton href="/plugins/examples/" variant="secondary" icon="book">Examples</LinkButton>
<LinkButton href="/plugins/registry/" variant="secondary" icon="magnifier">Plugin Registry</LinkButton>
